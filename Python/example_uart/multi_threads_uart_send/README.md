| PY文件 | 作用 |
| --- | --- |
| main.py | 主程序 |
| Timer.py | 定时器类，使用Thread模块来实现定时调用某条函数的功能 |
| UartStream | 串口流类，多个线程同时调用发送数据帧、接收数据帧 |

## 现象
- 每相隔指定时间，在不同的线程发送一次数据
- 在主函数的循环中，不断读取串口流中的数据，解析出数据帧，并处理不同的状态